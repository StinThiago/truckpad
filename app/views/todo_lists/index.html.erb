<% @todo_lists.each do |todo_list| %>
  <div class="index_row clearfix">
    <h2 class="todo_list_title"><%= link_to todo_list.description, todo_list %></h2>
    <p class="todo_list_sub_title"><%= false ? User.find(@todo_list.user_id).first_name : 'Nome não cadastrado' %> | <%= todo_list.due_date %></p>
  </div>
<% end %>

<div class="links">
  <%= link_to "Nova lista", new_todo_list_path %>
</div>
